package org.ds.algos.practice.ds_algos.trees;

/**
 * Convert a binary tree to a doubly linked list
 * The order of the linked list will be that of inorder traversal
 ** INCOMPLETE **
 */
public class TreeToDLL {

    /**
     * Takes the root of the binary tree and returns the head of the
     * doubly linked list
     * @param root
     * @return
     */
    public Tree.Node treeToDoublyLinkedList(Tree.Node root) {
        if(root == null)
            return null;

        return null;
    }

    public static void main(String[] args) {

    }
}
